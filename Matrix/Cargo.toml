[package]
name = "Matrix"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = { version = "0.29", default-features = false, features = ["events"] }
rand = "0.10.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
